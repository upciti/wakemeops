name: fzf
matrix:
  architectures:
    - amd64
    - arm64
  versions:
    - 1.3.0
homepage: https://github.com/sachaos/viddy
summary: A modern watch command. Time machine and pager etc
description: |-
  Modern watch command.
  Viddy well, gopher. Viddy well.
fetch:
  url: https://github.com/sachaos/viddy/releases/download/v{{version}}/viddy-v{{version}}-{{target|rust_target}}.tar.gz
  targets:
    amd64: linux-x86_64
    arm64: linux-arm64
install:
  - viddy-v{{version}}-linux-{{target|rust_target}}/viddy:/usr/bin/
